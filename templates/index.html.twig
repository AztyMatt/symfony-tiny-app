{% extends 'parts/base.html.twig' %}

{% block title %}NextTask{% endblock %}

{% block javascripts %}
{% endblock %}

{% block body %}
    <div class="w-fit flex flex-col items-center justify-center h-full gap-8">
        <div class="flex flex-col gap-1">
            <h1 class="text-4xl font-bold">Welcome to NextTask</h1>
            <p class="text-lg">NextTask is a simple task manager.</p>
        </div>
        <div class="w-full flex flex-row gap-2">
            <a href="{{ path('login') }}" class="px-4 py-2 bg-blue-500 text-white rounded w-full text-center">Login</a>
            <a href="{{ path('register') }}" class="px-4 py-2 bg-blue-500 text-white rounded w-full text-center">Register</a>
        </div>
    </div>
{% endblock %}
