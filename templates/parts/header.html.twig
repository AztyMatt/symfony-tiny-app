<header class="bg-gray-800 text-white p-4 flex justify-between items-center overflow-hidden">
    <h1 class="text-2xl"><a href="/">NextTask</a></h1>
    {% if app.user %}
        {# <div class="relative w-1/2">
            <input type="text" placeholder="Search..." class="p-2 border border-gray-300 shadow w-full rounded">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="absolute right-2 top-1/2 transform -translate-y-1/2 h-6 w-6 text-gray-500">
                <path stroke-linecap="round" stroke-linejoin="round" d="m21 21-5.197-5.197m0 0A7.5 7.5 0 1 0 5.196 5.196a7.5 7.5 0 0 0 10.607 10.607Z" />
            </svg>
        </div> #}
        <a href="{{ path('app_logout') }}" class="flex flex-row items-center gap-4 group">
            <span class="group-hover:text-red-500 transition duration-300">{{ app.user.email }}</span>
            <img src="{{ app.user.profilePicture }}" alt="Profile Image" class="rounded-full h-10 w-10 object-contain group-hover:opacity-50 transition duration-300">
        </a>
    {% endif %}
</header>
