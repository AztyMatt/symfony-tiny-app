{% extends 'parts/base.html.twig' %}

{% block title %}NextTask-Admin - Dashboard{% endblock %}

{% block javascripts %}
{% endblock %}

{% block body %}
    <header class="p-4 w-full flex justify-between items-center border-b-2 border-gray-200 shadow sticky top-0 bg-white">
        <h1 class="text-2xl font-bold">Admin Dashboard</h1>
    </header>

    <div class="flex flex-col overflow-y-auto h-full w-full">
         <div class="flex flex-row gap-4 w-full p-8 bg-gray-100 shadow">
            <a href="{{ path('app_team_index') }}" class="px-4 py-2 bg-blue-500 text-white rounded">Teams</a>
            <a href="{{ path('app_project_index') }}" class="px-4 py-2 bg-blue-500 text-white rounded">Projects</a>
        </div>
    </div>
{% endblock %}
